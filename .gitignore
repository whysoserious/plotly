# Rust
/target/
**/*.rs.bk
*.pdb
# Note: Cargo.lock is intentionally committed (this is a binary/application, not a library).

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.Spotlight-V100
.Trashes
.DocumentRevisions-V100
.fseventsd
.TemporaryItems
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Plotly runtime output (default log is ./plotly.log; --log-file may add others)
*.log
